console.log("JavaScript Challenge - Open the console (F12) to see your progress!");

// --- Challenge: User Age Verification and Access Control ---
// 
// Description: 
// You are building a simple system that verifies a user’s age and grants access based on their age. 
// The program should: 
// 
// 1. Ask the user to enter their age (use prompt()).
// 2. Convert the input into a number (use parseInt() or Number()).
// 3. Check the following conditions:
//    - If the age is 18 or older, log: "Access granted."
//    - If the age is between 13 and 17 (inclusive), log: "Access granted with restrictions."
//    - If the age is below 13, log: "Access denied."
// 4. If the user enters a non-numeric value or leaves it empty, log: "Invalid input, please enter a number."
//
// --- Hint: ---
// - Use `prompt()` to take input from the user.
// - Convert the input to a number using `Number()` or `parseInt()`.
// - Use `if`, `else if`, and `else` statements to check the age conditions.
// - Use `console.log()` to display the results.
